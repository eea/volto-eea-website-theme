--- volto-18.35.1-74d82edef/packages/volto/src/components/manage/Sidebar/SidebarPopup.jsx
+++ volto-eea-website-theme/src/customizations/volto/components/manage/Sidebar/SidebarPopup.jsx
@@ -1,3 +1,5 @@
+// Backport of https://github.com/plone/volto/pull/5520 for Volto 17.
+// On Volto 18 this shadow is identical to upstream — keep while V17 is supported.
 import React from 'react';
 import { createPortal } from 'react-dom';
 import { CSSTransition } from 'react-transition-group';
@@ -36,6 +38,7 @@
       document.removeEventListener('keyup', handleEscapeKey, false);
     };
   });
+
   return (
     <>
       {overlay && (
